- hosts: all
  become: yes
  tasks:
   - name: Update package
     apt:
        update_cache: yes
   - name: Install Git
     command: apt install git -y
     register: git_version
   
   - name: Printing Git Version
     debug: 
      var: git_version.stdout
   
